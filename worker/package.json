{
  "name": "packetbracket-geo-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker geo-intelligence proxy for PacketBracket",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
